{{> header}}
<div id="car-listings">
    <h1>Car Listings</h1>
    <div id="listings">
        {{#if noCars}}
        <p class="no-cars">No cars in database.</p>
        {{else}}

        {{> listCars}}
        
        {{/if}}
    </div>
</div>
{{> footer}}